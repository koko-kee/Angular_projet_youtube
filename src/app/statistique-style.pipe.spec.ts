import { StatistiqueStylePipe } from './statistique-style.pipe';

describe('StatistiqueStylePipe', () => {
  it('create an instance', () => {
    const pipe = new StatistiqueStylePipe();
    expect(pipe).toBeTruthy();
  });
});
